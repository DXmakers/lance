name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  # Real Cloud SQL database for Prisma generate/type checks
  DATABASE_URL: postgresql://postgres:jgvdVyC3fuqrJOPQ435K@35.253.43.59:5432/lance?schema=public
  # Real Cloud Run Backend URL for the Frontend
  NEXT_PUBLIC_API_URL: https://lance-api-641037923100.us-central1.run.app
  
  # Existing mock/contract env vars
  JUDGE_AUTHORITY_SECRET: SBU6F23AV5T5E6AXK2J5C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6
  ESCROW_CONTRACT_ID: CD5E6AXK2J5C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6
  REPUTATION_CONTRACT_ID: CD5E6AXK2J5C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6
  JOB_REGISTRY_CONTRACT_ID: CD5E6AXK2J5C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6C6A6
  OPENCLAW_API_KEY: test_key
  OPENCLAW_BASE_URL: http://localhost:8080

jobs:
  backend:
    name: Node.js Backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm install --prefix backend
      - name: Generate Prisma Client
        run: npx prisma generate --schema=backend/prisma/schema.prisma
      - name: Build
        run: npm run build --prefix backend

  web-lint-build:
    name: Web Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm install --prefix apps/web
      - name: Lint
        run: npm run lint --prefix apps/web
      - name: Build
        run: npm run build --prefix apps/web
